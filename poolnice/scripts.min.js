function fadeOut(e) {
  (e.style.opacity = 1),
    (function t() {
      (e.style.opacity -= 0.1) < 0
        ? (e.style.display = "none")
        : requestAnimationFrame(t);
    })();
}
function bindModal(e, t, l) {
  (e = document.querySelectorAll(e)),
    console.log('e: ', e);
  (t = document.querySelector(t)),
    (l = document.querySelector(l));
  const o = document.body;
  for (var i = 0; i < e.length; i++) {
    e[i].addEventListener("click", function (e) {
      (t.style.display = "flex"), o.classList.add("locked");
    });
  }
  l.addEventListener("click", () => {
    (t.style.display = "none"), o.classList.remove("locked");
  }),
    t.addEventListener("click", (e) => {
      e.target === t &&
        ((t.style.display = "none"), o.classList.remove("locked"));
    });
}
bindModal(".openmodtwo, .btn.btn-base.btn-small--mobile.btn-primary.cm-button--min-width, .logmodal, .opreact ", ".modal__wrapper", ".modal__close"),
  bindModal(".nextModal", ".game-modal__wrapper", ".game-modal__close");

var acc = document.getElementsByClassName("modal__acrdn");
var panel = document.querySelectorAll(".modal__body");
for (var i = 0; i < panel.length; i++) {
  panel[i].setAttribute("style", "max-height:" + panel[i].scrollHeight + 80 + "px");
}
var i;

jQuery(".opreact").on("click", function () {
  jQuery('#react-modal-rootywo').css('display', 'none');
  jQuery('div#react-modal-root').css('display', 'none');
});
for (i = 0; i < acc.length; i++) {
  acc[i].addEventListener("click", function () {
    this.classList.toggle("active");
    var panel = this.nextElementSibling;
    if (this.classList.contains("active")) {
      panel.style.maxHeight = panel.scrollHeight + 20 + "px";
    } else {
      panel.style.maxHeight = 0 + "px";
    }
  });
}